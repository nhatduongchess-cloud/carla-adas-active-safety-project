# ==============================================================================
# CARLA ADAS — .gitignore
# ==============================================================================

# --- Virtual environments -----------------------------------------------------
.venv/
.venv-1/
.venvCarLa/
myenv/
env/
venv/
ENV/
*.egg-info/

# --- Python bytecode / caches -------------------------------------------------
__pycache__/
*.py[cod]
*$py.class
.pytest_cache/
.mypy_cache/
.ipynb_checkpoints/

# --- Editor / IDE / agent config ----------------------------------------------
.vscode/
.idea/
.claude/
*.code-workspace

# --- Third-party CARLA repositories (cloned separately — see README) ----------
# These are large upstream projects with their own git history & licenses.
rllib-integration/
ros-bridge/
scenario_runner/
transfuser/

# --- Datasets & training output (regenerable — see README) --------------------
dataset/
carla_dataset_v1/
# Ultralytics detector training / validation runs (curves, batch previews)
runs/

# --- Model weights ------------------------------------------------------------
# Standard detector weights are downloadable; do not bloat the repo with them.
*.pt
*.pth
*.onnx
*.engine
*.trt
# CLIP ViT-B-32.pt (~354 MB) used by YOLO-World open-vocabulary detection
weights/clip/
# ...but DO keep our own tiny trained RL policy so demos run out of the box.
!weights/rl_speed_policy.pt

# --- Runtime output / logs / telemetry ----------------------------------------
logs/
telemetry_log_*.csv
*.log

# --- Bản nháp cũ đã bị thay thế -----------------------------------------------
# main.py là draft cũ (import 'midas_depth' không tồn tại, sai chữ ký
# ActiveSafetySystem) -> chinh.py là entry point thực. Không đưa lên repo.
/main.py

# --- OS / cloud-sync junk -----------------------------------------------------
.DS_Store
Thumbs.db
desktop.ini
ehthumbs.db

# --- Portfolio-public exclusions ---------------------------------------------
# V&V evidence, scratch and dev-setup template: kept local, summarised in docs/.
output/
tmp/
Setup codex/
Claude outputs/

# Internal working docs (kept local, not published to the portfolio repo)
CLAUDE_HANDOFF.md
docs/LUNA_END_TO_END_RECOVERY_PLAN.md
docs/SOL_END_TO_END_EXECUTION_PLAN.md
docs/TWO_WEEK_RESOURCE_SCALED_EXECUTION.md
docs/ENGINEERING_PROGRESS.md
docs/BUG_LEDGER.md
docs/PORTFOLIO_SCOPE_PLAN.md
docs/README_original.md
docs/README_PORTFOLIO_DRAFT.md
