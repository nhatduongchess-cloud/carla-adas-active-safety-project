# ==============================================================================
# CARLA ADAS — Python dependencies (tested on Python 3.12, Windows / Linux)
# ==============================================================================
# The CARLA client library (`carla`) is NOT installed from here. It ships with
# the CARLA server release and must match your server version. Install it with:
#     pip install carla==0.9.14
# or from the wheel in <CARLA>/PythonAPI/carla/dist/ if the pip version is
# unavailable for your platform.
# ------------------------------------------------------------------------------

# --- Core numerics & vision ---------------------------------------------------
numpy>=1.26,<2.0
opencv-python>=4.9.0
scikit-learn>=1.4.0          # DBSCAN clustering for the LiDAR forward corridor

# --- Deep learning / detection ------------------------------------------------
torch>=2.1.0                 # install the CUDA build for GPU (see README)
torchvision>=0.16.0
ultralytics>=8.2.0           # YOLOv8/v10 detector ensemble

# --- Configuration & reporting ------------------------------------------------
PyYAML>=6.0                  # weather / ODD profile configs
matplotlib>=3.8.0            # RL reward curve, plots

# --- Optional: monocular depth (MiDaS) ----------------------------------------
# Only needed when config.USE_MIDAS = True (depth is off the braking path).
timm>=0.9.0

# --- Optional: inference acceleration (scripts/training/export_models.py) ----------------------
# Uncomment the one matching your hardware to run the optimized YOLO path.
# onnx>=1.15.0
# onnxruntime-gpu>=1.17.0
# tensorrt>=8.6.0
